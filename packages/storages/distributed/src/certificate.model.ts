import type { ICertificate } from '@scrapoxy/common';


export interface ICertificateModel extends ICertificate {
    // eslint-disable-next-line @typescript-eslint/naming-convention
    _id: string;
}
